These are cedes of the first case, namely, TL case.

test1.m is employed to learn the optimal state-action function

cluster_Algorithm is used to check the nonblocking property, verify the satisfactory of control requirements, 
and clollect the nonblocking reachable states.
